<h1><%= @task.title %></h1>

<p><%= @task.details %></p>

<p><%= @task.completed ? 'fait' : 'à faire'%></p>
<button>
  <%= link_to "Delete : #{@task.title}",
    task_path(@task),
    method: :delete,
    data: { confirm: "Are you sure?" }
  %>
</button>

<button><%= link_to "edit", edit_path(@task) %></button>
<button><%= link_to "retour", tasks_path %></button>
